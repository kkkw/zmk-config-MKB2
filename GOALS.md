# 現在の目標 (Goals)

## 達成したいこと

1. **安定したBluetooth接続の確立**
   - チャタリング（連続入力）が発生しないこと
   - 接続のフリーズ（キーが反応しなくなる状態）が発生しないこと
   - ストレスなく日常使用できる安定性を確保すること

2. **左手エンコーダーのスクロール機能実装**
   - 左手のエンコーダーに「スクロールアップ」「スクロールダウン」を割り当てること
   - こちらも操作時にストレス（遅延や無反応など）がない状態にすること

## 現状の課題と注意点

上記の目標を達成するための設定変更（スリープ設定、デバウンス、エンコーダーのタップ時間、Bluetoothバッファなどの調整）を行うと、以下のような深刻な副作用が発生しやすく、非常に困っている状態である。
* 全体的なチャタリングの発生
* 左右または片手のフリーズ
* キーボード自体が起動しなくなる（文鎮化・ブート失敗）

## 今後の方針

* トラブルシューティングのドキュメント（`docs/troubleshooting-ble-split.md` など）を踏まえつつ、**副作用が出ないように** 慎重に設定（`west.yml` の ZMK リビジョン、`CONFIG_*` の調整、エンコーダーの `tap-ms` などの調整）を行っていく。
* 設定変更時は必ず一つずつ影響を確認し、動かなくなった場合はすぐに元に戻せるようにする。
