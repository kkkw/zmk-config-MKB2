# エージェント作業規約

このリポジトリは、分割型モジュラーキーボード「メカブ（MeKaBu / MKB2）」の ZMK 設定を管理する。リポジトリの利用者は、必ずしもファームウェアやハードウェアの制作者ではない。エージェントは、利用者が求める実際のキーボード動作を確認し、安全で元に戻せる小さな変更を積み重ねること。

## 最終ゴールの確認

ファームウェアの動作を変更する前に、必ず `GOALS.md` と利用者の最新の依頼を確認し、今回達成する状態と成功の判定方法を具体化する。

`GOALS.md` と最新の依頼が矛盾する場合は最新の依頼を優先する。ただし、利用者から依頼されていない限り、エージェントの判断だけで `GOALS.md` を変更しない。

現時点で確認されている目標は次のとおり。

- Bluetooth 接続を安定させ、チャタリング、フリーズ、起動不能を防ぐ。
- 左手側エンコーダーで上下スクロールを滑らかに動作させる。
- 原因の切り分けを難しくする広範囲な設定変更を避ける。

## プロジェクト構成

- `config/MKB.keymap`: メインの ZMK キーマップ。周辺ファイルから別の構成が確認されない限り、キー割り当て、レイヤー、コンボ、マクロ、エンコーダー動作の主な編集対象とする。
- `boards/shields/MKB/`: シールドのハードウェア定義と、各モジュール用の `.conf` / `.overlay`。起動、左右分割通信、センサー、エンコーダー、ポインティングデバイスに影響する可能性がある。
- `build.yaml`: ファームウェアのビルド対象一覧。シールド名と `boards/shields/MKB/` 以下のモジュール名の整合性を保つ。
- `keymap-drawer/` と `keymap-svg/`: キーマップ図の生成元と生成結果。
- `firmware/`: 生成またはダウンロードされた UF2 ファイル。設定変更の正本として扱わない。
- `skills/`: キーマップ図の生成やファームウェア書き込みなど、繰り返し作業の手順書。
- `docs/operations/`: 利用者が後から参照するビルド、書き込み、復旧などの運用手順。
- `docs/agent/`: エージェントが調査した資料、根拠、判断履歴を保存する領域。
- `docs/**`: 上記を含む、このフォーク固有の保護対象。

## 上流同期・リベース規約

このリポジトリは `upstream/main` の更新を継続的に取り込むフォークである。上流への追従と、このフォーク固有の文書・手順の保持を別の責務として扱う。

次のパスを保護対象とする。利用者が明示的に削除を依頼しない限り、上流同期、リベース、競合解消の過程で削除しない。

- `AGENTS.md`
- `GOALS.md`
- `docs/**`
- `skills/**`

保護対象を確実に残すには、ファイルが Git にコミットされている必要がある。未追跡ファイルはリベース対象にならず、ブランチの作り直し、`git clean`、作業ディレクトリの再作成などで失われ得る。保護対象を作成または更新した作業では、最終報告に追跡状態を記載し、未追跡のままならコミットが必要であることを明示する。

上流同期は原則として次の順序で行う。

1. `git status --short` で作業ツリーを確認し、未完了の変更をコミットまたは退避する。利用者の変更を無断で破棄しない。
2. `git ls-files -- AGENTS.md GOALS.md docs skills` で保護対象が追跡されていることを確認する。
3. 現在の `HEAD` を指す退避用ブランチを作り、同期前の状態へ戻れるようにする。
4. `git fetch upstream` 後、ローカルの変更コミットを `upstream/main` へ `git rebase upstream/main` で載せ直す。
5. 競合時は、ファームウェア設定について上流変更とこのフォークの意図を個別に確認する。保護対象はローカル版を保持する。ただし、上流に同名ファイルが新設された場合は一方を機械的に選ばず、内容を統合する。
6. リベース後に保護対象の存在と追跡状態を再確認し、`git diff --name-status upstream/main...HEAD` でこのフォーク固有の差分を確認する。
7. ファームウェアに関係する差分へ、通常のビルド・実機検証を行う。

上流同期のために `git reset --hard` や `git clean` を使用しない。履歴を作り直す必要がある場合も、退避用ブランチから保護対象のコミットまたはファイルを復元してから完了とする。リベース中に保護対象が消えた場合は同期成功と判断しない。

## 外部モジュールに関する知見

- `mjmjm0101/zmk-input-processor-scroll-inertia` は、トラックボールなどの連続したポインティング入力から生成されるスクロールイベントへ慣性を加える入力プロセッサである。
- 現在の左手 ENC モジュールは EC11 ロータリーエンコーダーであり、`config/MKB.keymap` の `zmk,behavior-sensor-rotate` から `&msc` を実行する。入力経路が異なるため、上記の慣性モジュールをそのまま左手エンコーダーへ適用できるとは判断しない。
- 左手トラックボールは `MKB_L_TB.overlay` で `zmk,input-listener` とスクロール用入力プロセッサを使用しているため、上記モジュールの導入候補になり得る。
- 未検証の仮説: 上記モジュールは公式 ZMK v0.3.0 で動作確認されているが、このプロジェクトは `te9no/zmk` の特定コミットと第三者製 PMW3610 ドライバーを使用している。導入前に API の互換性と、ドライバー側で慣性処理が重複しないことを確認する必要がある。
- `adolto/zmk-config-high-resolution-scroll-wheel` は EC11 の高解像度化ではなく、AS5600 磁気角度センサーの連続入力を `zmk,input-listener` からスクロールへ変換する実証実験である。現在の ENC モジュールへ設定だけを流用することはできない。

## エンコーダーと高解像度スクロールに関する知見

- `Kconfig.defconfig` により左手側が split central、右手側が peripheral である。ホストへ HID レポートを送る設定は原則として左手側へ適用する。
- 現在固定されている `te9no/zmk` のコミット `2ae914a28d179fffb8fdd72080e44ef719f49973` は `CONFIG_ZMK_POINTING_SMOOTH_SCROLLING` を実装している。
- `zmk,behavior-sensor-rotate` の `tap-ms` は、回転時に呼び出す behavior の押下から解放までの時間であり、既定値は 5ms である。回転トリガーごとに押下と解放の2イベントが behavior queue へ追加される。
- `&msc` の基盤である `zmk,behavior-input-two-axis` は、現在固定中の ZMK では既定16ms周期で整数のスクロールイベントを生成し、整数未満は内部の端数として次の周期へ繰り越す。behavior が解放されると端数は破棄される。
- 旧設定の `&msc SCRL_UP` / `SCRL_DOWN`（既定速度10）と `tap-ms = <100>` は、16ms周期の量子化により解放前の累計が通常0.96に留まり、整数イベントが発生しない可能性があった。
- 2026-08-29の通常解像度候補では `MOVE_Y(64)` / `MOVE_Y(-64)` と `tap-ms = <24>` を使用する。最初の16ms周期で1イベントを生成し、次の周期より前に解放する設計である。左右のローカルビルドと展開済みDeviceTreeは確認済みだが、スクロール量、方向、連続回転、Bluetooth安定性は実機未検証である。
- 長い `tap-ms` は回転ごとの解放を遅らせ、連続回転時に behavior queue の待ち時間と使用量を増やす。現在の既定キュー上限は64であり、`tap-ms` を安易に延ばさない。
- `&msc_input_listener` へ Input Processor を設定すれば、エンコーダーが生成したスクロールイベントにも倍率や方向変換を適用できる。ただし scaler は既に生成されたイベント量を変換するものであり、1イベントを時間方向に分割する機能ではない。
- 未検証の仮説: `CONFIG_ZMK_POINTING_SMOOTH_SCROLLING=y` と、1 ノッチの総移動量を維持する速度・`tap-ms` の組み合わせにより、EC11 の 1 ノッチを複数の高解像度 HID レポートへ分割できる可能性がある。有効化だけを行うとスクロール量が変わるため、必ず値をセットで調整する。
- 高解像度スクロールの有効化はマウス HID 記述子を変更する。Bluetooth 接続で検証する際は、古い記述子がホスト側に残らないよう設定リセット、既存ペアリングの削除、再ペアリングを試験手順に含める。
- 必要以上に短いスクロール生成周期や高いレポート頻度は、消費電力を増やし、BLE遅延の原因になり得る。Bluetooth安定化と両立させるため、既定16ms周期は速度・`tap-ms` だけでは目標を満たせないと実機で確認されるまで変更しない。
- `MKB_L_ENC.overlay` の `steps = <24>` と `triggers-per-rotation = <10>` は過去に複数の試行値を経た設定であり、採用理由はコミット履歴から確認できない。`triggers-per-rotation` は1回転あたりの behavior 発火回数なので、変更時は1周の物理ステップ数と画面上の発火回数を実測する。
- 安定化修正 `3e5b4c9` は ZMK fork を現在のコミットへ更新し、重複していた `CONFIG_ZMK_SPLIT_BLE_CENTRAL_SPLIT_RUN_STACK_SIZE=768` を削除した。エンコーダー調整時に ZMK revision や split/BLE stack 設定を同時に変更しない。
- 検証順序は、まずHID記述子を変えずに速度と短い `tap-ms` で通常解像度の1ノッチ出力を安定させ、その後、別の変更として高解像度スクロールを試す。
- 実機レビューでは標準構成が左エンコーダー・右トラックボールであることと、エンコーダーの物理回転が滑らかであることは確認できる。一方、公開記事には Bluetooth 安定化の具体的な設定値や、EC11 スクロール改善の検証済み設定値は見つかっていない。

## 安全な変更の原則

- ゴールに近づける可能性がある、最小限の変更を行う。
- 原則として、1 回の検証では 1 種類の挙動だけを変更する。利用者の明示的な依頼がない限り、Bluetooth、デバウンス、エンコーダー、ZMK リビジョンの調整を同時に行わない。
- 編集前に関連ファイルと既存の記述形式を確認し、プロジェクトの方針に合わせる。
- 利用者による変更を保持し、関係のない差分を上書きまたは巻き戻さない。
- 利用者の明示的な依頼がない限り、生成済みファームウェアを削除しない。
- 起動不能につながる可能性がある変更では、変更前または変更と同時に復旧方法を明記する。
- 実機へ書き込む前に、対象モジュールとブートローダー状態を利用者へ確認する。

## 検証ルール

- キーマップ変更後は、ローカルツールが利用可能なら次の手順で解析と図の再生成を行う。
  - `keymap parse -c keymap-drawer/config.yaml -z config/MKB.keymap > keymap-drawer/MKB.yaml`
  - `keymap draw -j keymap-drawer/config.yaml keymap-drawer/MKB.yaml > keymap-svg/MKB.svg`
- ビルド関連の変更は、このリポジトリに用意された GitHub Actions または既存の ZMK ビルド手順を優先して検証する。
- 実行できなかった検証がある場合は、未検証の項目と理由を作業報告に明記する。
- ファームウェアの挙動を変えた後は、変更箇所と回帰リスクに絞った短い実機確認項目を提示する。
- 実機でしか確認できない結果を、コード確認やビルド成功だけで「動作確認済み」と判断しない。

## 反復ビルド・書き込み

- 通常の push、定期実行、入力を変更しない手動実行は、`MKB_L_MODULE_ENC` と `MKB_R_MODULE_TBv4` の2ターゲットだけをビルドする。全ターゲットが必要な手動実行では workflow_dispatch の `target` に `all` を指定する。左エンコーダーだけを試す場合は `^MKB_L_MODULE_ENC$`、`commit_firmware = false` を指定できる。
- 2026-08-28時点の全ターゲット Actions は約11〜13分である。左 ENC のビルドジョブ自体は約2分14秒であり、単体実行では west準備などを含めて約4分が目安となるが、実測後に更新する。
- より短い反復には `scripts/build-zmk-local.sh` を使う。Docker named volume `zmk-config-mkb2-workspace` に west依存関係、ccache、ターゲット別build directoryを保持する。`config/west.yml` の変更時だけ依存を更新し、通常は同じbuild directoryを再利用する。出力は `build/local/` に生成する。
- 2026-08-29の初期構築後、変更なしの左ENC単体は1.44秒、左ENCと右TBv4の連続ビルドは1.91秒だった。Docker起動状態や変更量で時間は変わるため目安とする。右TBv4の初回build directory生成は約20秒だった。
- ローカルビルドでは既存のdeprecated symbol、Kconfig依存不成立、devicetree unit-addressなどのwarningが出る。warningを無視する一般規則にはせず、新規warningとの差分を確認する。2026-08-29時点の既存設定はwarningを伴っても左右両ターゲットのUF2生成に成功する。
- UF2 の書き込みは、利用者が対象コントローラーをUSB接続してブートローダーモードへ入れ、macOS の `/Volumes` 以下に UF2 ドライブが現れたことを確認してから行う。対象モジュール名と UF2 ファイルを照合し、明示的な確認なしにコピーしない。
- 書き込み時は左右を同時接続せず、対象側だけをUSB接続する。通常の反復更新では `settings_reset` を使わない。リセットボタンの位置が不明な場合、購入者である利用者へ未確認のRST/GND短絡を指示しない。
- macOSからの書き込みには `scripts/flash-uf2-macos.sh` を使い、最初に `--dry-run` でUF2、期待するartifact名、ブートローダードライブを照合する。`scripts/flashgh.ps1` と `scripts/flash.ps1` はPowerShell/Windowsドライブ前提に加え、古い remote・branch・artifact 名、引数名の不一致があるため使用しない。
- 現在の開発機には Docker CLI/Desktop と GitHub CLI があるが、2026-08-28の確認時点では Docker daemon は停止中、`west` は未導入、GitHub CLI の認証は期限切れである。ローカル環境や認証状態は変わり得るため、使用前に再確認する。

## 自己進化・自己改善ルール

エージェントは、作業中に信頼できる新しい知見を得た場合、この `AGENTS.md` を改善する。次回以降のエージェントが同じ調査や失敗を繰り返さず、より安全かつ短時間でゴールへ近づける内容に保つこと。

次の場合は `AGENTS.md` の更新を検討する。

- 再利用できるトラブルシューティング手順について、成功または失敗が確認された。
- ファイルや設定項目の役割が、現在の説明より正確に判明した。
- ビルド、キーマップ図生成、ファームウェア書き込みの手順が変わった。
- ハードウェアやモジュールの名称、左右の対応関係、依存関係が明確になった。
- 変更によって、チャタリング、フリーズ、起動不能、エンコーダー遅延、Bluetooth 不安定化が発生または解消した。
- 利用者から、今後も継続して守るべき作業方針や好みが示された。
- 上流同期で保持すべき、このフォーク固有のファイルや復旧手順が判明した。

更新時は次のルールを守る。

- 一度限りのログではなく、今後も役立つ簡潔で具体的な規則を記録する。
- 曖昧な記述は、検証済みの条件、手順、期待結果を含む記述に置き換える。
- 既存の規則と重複または矛盾する場合は、追記だけで済ませず整理・統合する。
- 推測に基づく内容は「未検証の仮説」と明記し、検証後に確定または削除する。
- 失敗事例を記録する場合は、症状、原因、避けるべき変更、復旧方法のうち判明しているものを残す。
- 秘密情報、アクセストークン、一時的な成果物名、不要な端末固有パスを記録しない。
- `AGENTS.md` の更新自体が目的にならないよう、利用者の最終ゴールに役立つ知見だけを残す。
- `AGENTS.md` を変更した場合は、最終報告で変更理由を利用者へ伝える。

## 作業時の対話

- 利用者から指定がない限り、日本語で対話する。
- 作業開始時に、改善したい挙動、成功の確認方法、問題発生時の戻し方を確認する。
- リスクのあるファームウェア変更は、適用前に影響範囲を説明する。
- 最終報告では、変更ファイル、実施した検証、残っているリスク、次に推奨する手順を簡潔に伝える。
